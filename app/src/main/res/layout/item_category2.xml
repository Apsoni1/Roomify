<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="8dp">

    <!-- Circular Background -->
    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/circleBg1"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginTop="16dp"
        android:src="@color/LightGray"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Furniture Image (e.g., Chair) -->
    <ImageView
        android:id="@+id/imageFurniture"
        android:layout_width="110dp"
        android:layout_height="130dp"
        android:layout_marginBottom="-4dp"
        android:background="@drawable/chair"
        app:layout_constraintBottom_toBottomOf="@id/circleBg1"
        app:layout_constraintStart_toStartOf="@id/circleBg1" />

    <!-- Shadow under item -->
    <ImageView
        android:id="@+id/imageShadow"
        android:layout_width="90dp"
        android:layout_height="40dp"
        android:alpha="0.5"
        android:src="@drawable/shadow"
        app:layout_constraintBottom_toBottomOf="@id/circleBg1"
        app:layout_constraintStart_toStartOf="@id/circleBg1" />

    <!-- Category Title -->
    <TextView
        android:id="@+id/textCategory"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="Chairs"
        android:textColor="@android:color/black"
        android:textSize="16sp"
        android:fontFamily="@font/roboto_regular"
        app:layout_constraintTop_toBottomOf="@id/circleBg1"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
